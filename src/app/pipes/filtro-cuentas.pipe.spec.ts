import { FiltroCuentasPipe } from './filtro-cuentas.pipe';

describe('FiltroCuentasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCuentasPipe();
    expect(pipe).toBeTruthy();
  });
});
